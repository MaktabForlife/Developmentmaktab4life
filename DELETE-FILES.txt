V101.3

No repository files or Google Sheet tabs should be deleted in this release.

Create the new `AdminAuditLog` tab and append the V101.3 audit columns before
deploying the Worker. Keep `SystemConfig`, `TimeTable` and `TeacherAssign`.
