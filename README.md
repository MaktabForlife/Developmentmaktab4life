# Maktab4Life V102.12.7

Changed-files release from V102.12.6. Apply the overlay to the deployed V102.12.6 development repository and deploy Pages + Worker from the same commit.

V102.12.7 is a focused correction release for publishable `TBA` Global Course sessions and Academic Calendar toolbar/mobile Holiday presentation. It does not implement the V103 unified Academy Access Matrix and does not change the Platform Sheet schema.

Keep `PlatformConfig!B3 = 102.0.8` with 19 required tabs.
