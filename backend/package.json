{
  "name": "rebootworker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=\"\" --keep-vars",
    "deploy:development": "wrangler deploy --env development --keep-vars",
    "test:google-sheets": "node tests/google-sheets-client.test.mjs",
    "test:weekly-planner": "node tests/weekly-planner.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
